{
  "schemaVersion": "1.0",
  "name": "parse",
  "domain": "render",
  "tool": "wardley-map",
  "description": "Parse an existing OWM DSL string into a WardleyChainAST. Used as the read-side entry for any analysis that operates on an existing map.",
  "steps": [
    {
      "stepId": "parse",
      "tool": "render:wardley-map:owm:parse:dsl",
      "in": "$.input",
      "out": "$.chain"
    }
  ],
  "listeners": []
}
