# @formicoidea/* is published to the public npm registry (registry.npmjs.org).
# The GitHub Packages routing below is kept commented for reference only —
# re-enable it if you ever need to consume a @formicoidea package from
# GitHub Packages instead of npmjs.
# @formicoidea:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
