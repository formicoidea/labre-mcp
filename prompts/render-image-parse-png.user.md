The attached {{mediaType}} image is a Wardley Map.

Transcribe it: read every component dot, its label, its position on the two
axes, and every dependency line drawn between two dots.

Return the JSON object between MAP_START and MAP_END, and nothing else.
