# ── Dependencies ──────────────────────────────
node_modules/

# ── Build output ──────────────────────────────
dist/
*.tsbuildinfo

# ── Turborepo ────────────────────────────────
.turbo/

# ── Environment & secrets ─────────────────────
.env
.env.local
.env.*.local

# ── Per-user LLM routing config ───────────────
# llm.config.json is per-machine: each user picks a profile from
# llm.config.example.json and adapts it locally (pick provider, models,
# effort, etc.). The canonical example stays tracked.
llm.config.json

# ── Package manager ───────────────────────────
# pnpm is the project's package manager; ignore npm/yarn lockfiles
package-lock.json
yarn.lock

# ── IDE / Editor ──────────────────────────────
.vscode/
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# ── OS files ──────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# ── Claude Code ───────────────────────────────
.claude/
.omc/

# ── fallow analysis dumps ─────────────────────
.fallow-*.json
.fallow/

# ── Logs & debug ──────────────────────────────
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# ── Synthetic round-trip dataset (npm run dataset) ──
dataset/

# ── Placement bench run artefacts (bench/run-pilot.mts) ──
# One file per campaign. Evidence of a run, not a tracked source: the tracked
# reference is bench/gold/gold-set.json. Attach a run to a PR or an ADR by hand
# when it is the thing being argued about.
bench/runs/

# ── Test & coverage ───────────────────────────
coverage/
.nyc_output/

# ── Temporary files ───────────────────────────
tmp/
tmp/renders/
*.tmp

# Exclude large output files
evaluations/*.html
evaluations/*.json

# But keep summary reports
!evaluations/summary-*.csv